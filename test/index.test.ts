import { describe, expect, it } from 'vitest'

describe('should test 1', () => {
  it('exported', () => {
    expect(1).toEqual(1)
  })
})

describe('should test 2', () => {
  it('exported', () => {
    expect(2).toEqual(2)
  })
})
